#type Mutation {
#	requestLogin(
#	    email: String!
#	): Result!
#}

type Query {
	meInfo: User!
	ideas: IdeaConnection
	getIdea(ideadId: String!): Idea!
}


type Idea {
	idea_id: String!
	content: String
	title: String
	user_id: String
	created_date: String
	idea_date: String
}


type User {
	user_id: String!
	email: String!
	ideas(limit: Int!, nextToken: String): IdeaConnection
}

type Result {
	error: String
	ok: Boolean
}


type  IdeaConnection {
	items: [Idea!]!
	nextToken: String
}

schema {
	query: Query
#	mutation: Mutation
}
