{
    "version" : "2017-02-28",
    "operation" : "UpdateItem",
    "key" : {
        "userId" : { "S" : "${context.identity.username}" }
    },
    "update" : {
        "expression" : "SET followedUsers = $util.list.copyAndRemoveAll(followedUsers, :userToDelete),
        "expressionValues": {
        	":user" : {"L":  [{"S": "${context.identity.userId}"}]
        	},
        ":empty_list": {"L": []}
        }
    }
}
