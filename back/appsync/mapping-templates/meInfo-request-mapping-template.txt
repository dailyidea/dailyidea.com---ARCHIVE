{
    "version" : "2017-02-28",
    "operation" : "Query",
    "query" : {
        "expression": "userId = :handle",
        "expressionValues" : {
            ":handle" : {
                "S" : "${context.identity.username}"
            }
        }
    }
}
