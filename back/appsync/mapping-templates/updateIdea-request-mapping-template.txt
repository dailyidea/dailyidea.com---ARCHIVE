
{
    "version" : "2017-02-28",
    "operation" : "UpdateItem",
    "key" : {
        "ideaId" : { "S" : "${context.arguments.ideaId}" }
    },
    "update" : {
        "expression" : "SET tweet = :tweet",
        "expressionValues": {
        	":content" : { "S" : "${context.arguments.content}" },
        	":title" : { "S" : "${context.arguments.title}" },
        	":ideaDate" : { "S" : "${context.arguments.ideaDate}" }
        }
    }
}
