{
    "version" : "2017-02-28",
    "operation" : "UpdateItem",
    "key" : {
        "userId" : { "S" : "${context.identity.username}" }
    },
    "update" : {
        "expression" : "SET followedUsers = list_append(if_not_exists(followedUsers, :empty_list), :user)",
        "expressionValues": {
        	":user" :  {"L":
        	    [{"S": "${context.arguments.userId}"}]
        }
      }
    }
}
